A program, in which you can buy vignette for a car, or vignettes for all cars which you have. The vignettes are in GasStation class and when you generate a GasStation, there will be thousands vignettes. They can be for a car,truck and bus. Their periods are: DAYLY, MONTHLY, YEARLY. By math.random the vignettes is generated, 33% for every Vehicle type, and 33% for every period type. The price for every Vehicle type is different and the same for period type, using Enum. Using Lists.
